// Only change code below this line

function getRndInteger(min, max) {

    return Math.floor(Math.random() * (30 - 0 + 1) ) + min;

}

// Only change code above this line
module.exports = randomInteger;